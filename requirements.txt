Django>=4.2,<5.0

# Base de datos PostgreSQL
psycopg2-binary>=2.9.9
dj-database-url>=2.1.0

# Redis - Caché y Celery broker
redis>=5.0.0
django-redis>=5.4.0

# Celery - Tareas asincrónicas
celery>=5.3.0
celery[redis]>=5.3.0

# HTMX integration
django-htmx>=1.17.0

# Variables de entorno
python-dotenv>=1.0.0

# Pagos
mercadopago>=2.2.0
cryptography>=42.0.0
requests>=2.31.0

# Para desarrollo
django-debug-toolbar>=4.2.0

# Tailwind CSS (opcional - se puede usar CDN también)
# Nota: django-tailwind 3.7+ requiere Python 3.10+
django-tailwind>=3.5.0,<3.7.0

# Utilidades
django-extensions>=3.2.3

# Fix macOS LibreSSL warning: urllib3 v2 requiere OpenSSL 1.1.1+.
# Si tu Python está compilado con LibreSSL, pinneamos urllib3 < 2.
urllib3<2

# Producción - Servir archivos estáticos
whitenoise>=6.6.0

# Producción - Servidor WSGI
gunicorn>=21.2.0

# Imágenes (Pillow para ImageField)
Pillow>=10.0.0
