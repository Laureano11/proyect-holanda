# Configuración de Django
SECRET_KEY=t6&zx-cuj98a)a-47qw&!%8g2+(eou0j&av%9+-bo4gv0=8_9t
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Para usar con ngrok (evita 403 CSRF al registrarse/loguearse)
# Podés dejarlo vacío si no usás ngrok.
CSRF_TRUSTED_ORIGINS=https://*.ngrok-free.dev,https://*.ngrok-free.app

# Base de datos PostgreSQL
DB_NAME=turnos_db
DB_USER=lauri11
DB_PASSWORD=tu_password_aqui
DB_HOST=localhost
DB_PORT=5432

# Producción (Render) - alternativa recomendada: usar una sola URL
# DATABASE_URL=postgres://user:password@host:5432/dbname

# Redis - Caché y Celery (IMPORTANTE para producción)
# En desarrollo local: redis://localhost:6379/0
# En Render: automático cuando agregas Redis addon
REDIS_URL=redis://localhost:6379/0

# Configuración de Email (para recuperación de contraseñas)
# En desarrollo: 
#   - USE_RESEND_IN_DEV=False → Los emails se muestran en consola (por defecto)
#   - USE_RESEND_IN_DEV=True → Los emails se envían realmente con Resend
# En producción: siempre usa SMTP real

USE_RESEND_IN_DEV=False

# Opción RECOMENDADA: Resend (moderno, confiable, gratis 3K/mes)
EMAIL_HOST=smtp.resend.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=resend
EMAIL_HOST_PASSWORD=re_XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
DEFAULT_FROM_EMAIL=onboarding@resend.dev



# Integración Mercado Pago
MERCADOPAGO_ACCESS_TOKEN=mp_test_xxxxxxxxxxxxxxxxxxxxx
MERCADOPAGO_PUBLIC_KEY=TEST-xxxxxxxxxxxxxxxxxxxxxxxx
# OAuth Mercado Pago (marketplace)
MP_CLIENT_ID=
MP_CLIENT_SECRET=
# Debe coincidir EXACTO con la URL configurada en tu aplicación de Mercado Pago.
# Nota: el proyecto también acepta MP_REDIRECT_URL como alias.
MP_REDIRECT_URI=https://tudominio.com/mercadopago/oauth/callback/
# Clave para cifrado de tokens (Fernet 32 bytes base64)
FIELD_ENCRYPTION_KEY=

# Para webhooks en local, usá un túnel (ngrok) y setea estos valores con la URL pública:
# ALLOWED_HOSTS=localhost,127.0.0.1,tu-id.ngrok-free.app
# CSRF_TRUSTED_ORIGINS=https://tu-id.ngrok-free.app

SECRET_KEY_WEBHOOK= fea8205d25d7b2dda67124d6389d487a2d53717629b8e2be88b6c61d1f25dc9d